<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="220dp"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/category_frame"
        android:layout_width="match_parent"
        android:layout_height="220dp">

        <ImageView
            android:id="@+id/category_item_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/general_frame"
            android:scaleType="centerCrop"
            android:src="@drawable/restaurant_1" />

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/category_item" />

    </FrameLayout>

    <TextView
        android:id="@+id/category_item_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="8dp"
        android:layout_marginLeft="@dimen/margin_typical"
        android:layout_marginStart="@dimen/margin_typical"
        android:textColor="@color/colorTextFaded"
        android:textSize="@dimen/text_small"
        tools:text="Restaurant 1 Subtitle" />

    <TextView
        android:id="@+id/category_item_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/category_item_type"
        android:layout_marginLeft="@dimen/margin_typical"
        android:layout_marginStart="@dimen/margin_typical"
        android:textColor="@color/colorText"
        android:textSize="@dimen/text_large"
        tools:text="Restaurant 1 Name" />

</RelativeLayout>